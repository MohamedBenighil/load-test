apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-ssl
  # annotations:
  #   kubernetes.io/ingress.class: "nginx"
  #   cert-manager.io/cluster-issuer: letsencrypt # to delete
  #=======================================
  # nginx.ingress.kubernetes.io/proxy-buffer-size: "32k"
  # nginx.ingress.kubernetes.io/proxy-buffers-number: "4"
  # nginx.ingress.kubernetes.io/ssl-redirect: "true"

  # nginx.ingress.kubernetes.io/auth-cache-key: "$cookie__oauth2_proxy"
  # nginx.ingress.kubernetes.io/auth-cache-duration: "200 202 10m"
  # #
  # nginx.ingress.kubernetes.io/auth-url: "https://auth.mohamed-benighil.com/oauth2/auth"
  # nginx.ingress.kubernetes.io/auth-signin: "https://auth.mohamed-benighil.com/oauth2/start?rd=https%3A%2F%2F$host$request_uri"
  # nginx.ingress.kubernetes.io/auth-response-headers: "x-auth-request-user, x-auth-request-email, x-auth-request-access-token"
  # #
#=============================================
spec:
  rules:
    - host: benighil.westeurope.cloudapp.azure.com # to replace
      http:
        paths:
          - path: /locust
            pathType: Prefix
            backend:
              service:
                name: locust-master-ui
                port:
                  number: 8089
  # tls:
  # - hosts:
  #   - locust.mohamed-benighil.com  # to replace
  #   secretName: sslcertificate
